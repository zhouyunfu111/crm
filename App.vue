<script>
  import config from './config'
  import store from '@/store'
  import { getToken } from '@/utils/auth'

  export default {
    onLaunch: function() {
      this.initApp()
    },
    methods: {
      // 初始化应用
      initApp() {
        // 初始化应用配置
        this.initConfig()
        // 检查用户登录状态
        //#ifdef H5
        this.checkLogin()
        //#endif

      },
      initConfig() {
        this.globalData.config = config
      },
      checkLogin() {
        if (!getToken()) {
			this.$tab.reLaunch('/pages/login') 
        } else {
			this.$tab.reLaunch("/pages/index/index")
		}
      }
    }
  }
</script>

<style lang="scss">
  @import "@/uni_modules/uview-ui/index.scss";
  @import '@/static/scss/index.scss';
  @import './commons/css/base.scss';
  @import './commons/css/vue-global.scss';
  @font-face {
    font-family: 'PingFang';
    src: url('@/static/font/PingFang.ttf') format('ttf'); /* 为现代浏览器 */
    font-weight: normal;
    font-style: normal;
  }
  
  body {
    font-family: 'PingFang',PingFang SC;
  }
</style>
